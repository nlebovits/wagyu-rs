coverage:
  status:
    project:
      default:
        target: 80%
        threshold: 5%
    patch:
      default:
        target: 80%

ignore:
  - "crates/cli/**"
  - "tests/**"
  - "benches/**"

comment:
  layout: "reach,diff,flags,tree"
  behavior: default
  require_changes: false
